<UserControl x:Class="AppMMCV.View.Admin.MenuUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppMMCV.View.Admin"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBox Text="{Binding InfoMenu.Menu_name}" materialDesign:HintAssist.Hint="Menu name" 
                 Margin="5" Width="150" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
        
        <TextBox Text="{Binding InfoMenu.Menu_description}" materialDesign:HintAssist.Hint="Menu description" 
                 Margin="5" Width="150" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
        
        <TextBox Text="{Binding InfoMenu.Menu_index}" materialDesign:HintAssist.Hint="Menu index" 
                 Margin="5" Width="150" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
        
        <TextBox Text="{Binding InfoMenu.Menu_index}" materialDesign:HintAssist.Hint="Menu index" 
                 Margin="5" Width="150" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
    </StackPanel>
</UserControl>

<UserControl x:Class="AppMMCV.View.Admin.ResetPasswordUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AppMMCV.View.Admin"
             xmlns:materialDesign ="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <WrapPanel>
            <TextBox Name="txt_username" Margin="5" Width="200" materialDesign:HintAssist.Hint="Username" Style="{StaticResource MaterialDesignFilledTextBox}"/>
            <Button Click="Button_Click" Content="Cài lại mật khẩu mặc định" Margin="5"/>
            <TextBlock FontStyle="Italic" Margin="5" Text="Mật khẩu mặc định: mmcv123" VerticalAlignment="Center"/>
        </WrapPanel>
       
    </Grid>
</UserControl>
